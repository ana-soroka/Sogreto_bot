# 🧪 Инструкция по тестированию триггерной системы напоминаний

## ⚠️ ВАЖНО: Бот запущен в ТЕСТОВОМ РЕЖИМЕ

Планировщик настроен на проверку **каждую минуту** (вместо каждого часа).
Это позволяет быстро протестировать напоминания.

---

## 📋 Подготовка к тесту

### 1. Убедитесь, что вы завершили Этап 1 (посадку)

В Telegram боте:
```
/start_practice  (если ещё не начали)
[Пройдите все 6 шагов с кнопками]
```

### 2. Узнайте ваш User ID

Откройте PowerShell и выполните:
```powershell
cd C:\Users\Redmi\Documents\Projects\Sogreto_bot
python test_reminders.py show
```

Вы увидите ваш telegram ID (например: 1585940117)

---

## 🧪 Сценарии тестирования

### ТЕСТ 1: День 2 - "Пора проверить всходы!"

**Что тестируем:** Первое напоминание о всходах

**Команда:**
```powershell
python test_reminders.py simulate ВАШ_ID 2
```

**Пример:**
```powershell



```

**Что произойдёт:**
- Скрипт установит `started_at` на 2 дня назад
- Установит `preferred_time` на текущее время + 1 минута
- Через 1-2 минуты вы получите сообщение:

```
🌱 Пора проверить всходы!

Прошло 2 дня с момента посадки. Обычно в это время
появляются первые ростки.

Загляните в горшок - видите зелёные петельки?
Если да, продолжайте практики!

Используйте /status чтобы продолжить.
```

---

### ТЕСТ 2: День 3 - "Напоминание о всходах"

**Команда:**
```powershell
python test_reminders.py simulate ВАШ_ID 3
```

**Ожидаемое сообщение:**
```
🌿 Напоминание о всходах

Уже 3 дня с момента посадки. Проверьте горшок -
появились ли ростки?

Если нет - не переживайте, иногда семенам нужно
чуть больше времени.

/status - продолжить практики
```

---

### ТЕСТ 3: День 4 - "Проверка всходов"

**Команда:**
```powershell
python test_reminders.py simulate ВАШ_ID 4
```

**Ожидаемое сообщение:**
```
🌾 Проверка всходов

4-й день после посадки. Если ростки ещё не показались - проверьте:
• Достаточно ли влаги в почве?
• Накрыт ли горшок плёнкой?
• Стоит ли в тёплом месте?

/status - продолжить
```

---

### ТЕСТ 4: 🚨 День 5 - "Салат не всходит?" (КРИТИЧЕСКОЕ)

**Что тестируем:** Специальное напоминание о пересадке

**Команда:**
```powershell
python test_reminders.py simulate ВАШ_ID 5
```

**Ожидаемое сообщение:**
```
⚠️ Салат не всходит?

Прошло уже 5 дней с момента посадки. Если всходов так и нет, возможно:

• Семена оказались невсхожими
• Недостаточно влаги или света
• Слишком глубоко посажены

Рекомендация: Попробуйте пересадить салат заново:
1. Возьмите новые семена
2. Увлажните почву
3. Посадите неглубоко (1-2 мм)
4. Накройте плёнкой

После пересадки используйте /reset чтобы начать отсчёт заново.
```

---

### ТЕСТ 5: День 7 - "Время первого урожая!"

**Предварительно:** Переключитесь на Этап 3
```
В Telegram: продолжайте нажимать кнопки практик, пока не дойдёте до Этапа 3
```

**Команда:**
```powershell
python test_reminders.py simulate ВАШ_ID 7
```

**Ожидаемое сообщение:**
```
🌱 Время первого урожая!

Прошла неделя с момента посадки. Ваша микрозелень готова к первому сбору!

Переходите к Этапу 4 - практика «Якорь» и дегустация.

/status - продолжить
```

---

### ТЕСТ 6: Этап 5 - Ежедневные практики (дни 8-14)

**Предварительно:** Переключитесь на Этап 5

**Команда:**
```powershell
python test_reminders.py simulate ВАШ_ID 8   # День 1 из 7
python test_reminders.py simulate ВАШ_ID 9   # День 2 из 7
python test_reminders.py simulate ВАШ_ID 10  # День 3 из 7
```

**Ожидаемое сообщение:**
```
🌱 День X из 7: Ежедневная практика

Продолжайте свои ежедневные практики с большими целями.

Ваш беби-лиф растёт, и вместе с ним растут ваши намерения.

/status - сегодняшняя практика
```

---

## 🔍 Как проверить логи

Чтобы увидеть, что планировщик работает, смотрите логи бота:

```powershell
# В папке проекта
ls logs/
cat logs/bot.log | Select-String "scheduler"
```

Вы должны увидеть:
```
2026-01-14 10:00:00 - utils.scheduler - INFO - Проверка напоминаний для X пользователей
2026-01-14 10:00:01 - utils.scheduler - INFO - Отправлено триггерное напоминание пользователю 123456 (день 2, этап 1)
```

---

## ⚙️ Дополнительные команды

### Посмотреть всех пользователей:
```powershell
python test_reminders.py show
```

### Сбросить прогресс (начать заново):
```
В Telegram боте: /reset
```

---

## 🛑 После тестирования

**НЕ ЗАБУДЬТЕ** вернуть планировщик в нормальный режим!

Откройте `utils/scheduler.py` (строка 336) и измените:
```python
# БЫЛО (тестовый режим):
CronTrigger(minute='*')  # Каждую минуту

# ВЕРНУТЬ:
CronTrigger(minute=0)  # Каждый час в 00 минут
```

Затем перезапустите бота.

---

## 💡 Советы

1. **Подождите 1-2 минуты** после команды `simulate` - планировщик проверяет каждую минуту
2. Если напоминание не пришло - проверьте:
   - Установлено ли `preferred_time`? (команда `/set_time`)
   - Не на паузе ли пользователь? (команда `/resume`)
   - Смотрите логи: `logs/bot.log`
3. Можно запускать тесты несколько раз - скрипт каждый раз обновляет `preferred_time`

---

## ❓ Если что-то не работает

1. Проверьте, что бот запущен: `tasklist | findstr python`
2. Проверьте логи: `cat logs/bot.log`
3. Проверьте БД: `python test_reminders.py show`
4. Убедитесь, что завершили Этап 1 (6 шагов)

---

**Готово к тестированию!** 🚀

Начните с ТЕСТА 1 (День 2) и проверьте, приходит ли напоминание.
