# 🐍 Правильная установка Python - Визуальная инструкция

## Шаг 1: Удалить старый Python ❌

### Вариант А: Через Настройки (Рекомендуется)

1. Нажмите `Win + I` (или найдите "Параметры" в меню Пуск)
2. Нажмите **"Приложения"**
3. Нажмите **"Установленные приложения"** или **"Приложения и возможности"**
4. В поиске введите: `Python`
5. Найдите **Python 3.x** (издатель: Microsoft Corporation)
6. Нажмите **три точки** `...` → **Удалить**
7. Подтвердите удаление

### Вариант Б: Через cmd (Для опытных)

```cmd
winget uninstall Python.Python.3
```

---

## Шаг 2: Скачать правильный Python ⬇️

### Ссылка для скачивания:

🔗 **https://www.python.org/downloads/**

### Какую версию скачивать:

- ✅ **Python 3.12.x** (последняя стабильная) - РЕКОМЕНДУЕТСЯ
- ✅ **Python 3.11.x** (тоже подойдёт)
- ⚠️ НЕ Python 3.13 (слишком новая, могут быть проблемы)

### На сайте:

1. Нажмите большую жёлтую кнопку **"Download Python 3.12.x"**
2. Дождитесь скачивания (файл ~25 MB)
3. Откройте скачанный файл

---

## Шаг 3: Установить Python ⚙️

### ⚠️ САМОЕ ВАЖНОЕ - НЕ ПРОПУСТИТЕ!

Когда откроется окно установки:

```
┌─────────────────────────────────────────┐
│  Install Python 3.12.x                   │
│                                          │
│  [Install Now] (recommended)            │
│                                          │
│  Customize installation                 │
│                                          │
│  ☐ Install launcher for all users      │
│  ☑ Add python.exe to PATH   ← ВАЖНО!   │
└─────────────────────────────────────────┘
```

### 1️⃣ Поставьте галочку ВНИЗУ окна:

```
☑ Add python.exe to PATH
```

**ЭТО КРИТИЧЕСКИ ВАЖНО!** Без этой галочки Python не будет работать из командной строки.

### 2️⃣ Нажмите "Install Now"

Не нужно выбирать "Customize installation", стандартная установка подходит.

### 3️⃣ Дождитесь завершения

Установка займёт 1-2 минуты.

### 4️⃣ Нажмите "Close"

---

## Шаг 4: Проверка установки ✅

### Закройте ВСЕ старые окна PowerShell/CMD!

Старые окна не увидят новый Python.

### Откройте НОВОЕ окно PowerShell:

1. Нажмите `Win + X`
2. Выберите **"Windows PowerShell"** или **"Терминал"**

### Проверьте Python:

```powershell
python --version
```

**Ожидаемый результат:**
```
Python 3.12.1
```

### Проверьте pip:

```powershell
python -m pip --version
```

**Ожидаемый результат:**
```
pip 24.0 from C:\Users\...\Python312\lib\site-packages\pip (python 3.12)
```

### Проверьте путь установки:

```powershell
where python
```

**Правильный результат:**
```
C:\Users\Redmi\AppData\Local\Programs\Python\Python312\python.exe
```

**Неправильный результат (НЕ ДОЛЖНО БЫТЬ):**
```
C:\Users\Redmi\AppData\Local\Microsoft\WindowsApps\python.exe  ❌
```

---

## Шаг 5: Установить зависимости бота 📦

После успешной проверки:

```powershell
cd C:\Users\Redmi\Documents\Projects\Sogreto_bot
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

Или просто запустите файл:
```
install_simple.bat
```

### Проверка установки зависимостей:

```powershell
python check_setup.py
```

Должно показать:
```
✅ Все зависимости установлены!
```

---

## Возможные проблемы и решения 🔧

### Проблема 1: "python не является внутренней командой"

**Причина:** Забыли поставить галочку "Add to PATH"

**Решение:**
1. Удалите Python (через Параметры → Приложения)
2. Установите заново
3. **НЕ ЗАБУДЬТЕ ГАЛОЧКУ!** ☑ Add python.exe to PATH

---

### Проблема 2: Python показывает версию, но pip не работает

**Решение:**
```powershell
python -m ensurepip --upgrade
python -m pip install --upgrade pip
```

---

### Проблема 3: Где находится галочка "Add to PATH"?

Галочка находится **В САМОМ НИЗУ** окна установки, под кнопкой "Customize installation".

Если вы её не видите - прокрутите окно вниз или разверните его на полный экран.

---

### Проблема 4: У меня установлено несколько версий Python

**Решение:**

1. Удалите ВСЕ версии Python:
   - Через Параметры → Приложения
   - Найдите все записи "Python 3.x"
   - Удалите каждую

2. Установите только одну последнюю версию

---

## Следующие шаги после установки 🚀

После успешной установки Python и зависимостей:

### 1. Инициализировать базу данных:
```powershell
python models.py
```

### 2. Запустить бота:
```powershell
python bot.py
```

Или просто запустите файл:
```
run_bot.bat
```

### 3. Протестировать в Telegram:
- Найдите своего бота
- Отправьте `/start`
- Попробуйте команды `/help`, `/status`

---

## Контрольный чек-лист ✓

Перед тем как продолжить, убедитесь:

- [ ] Python от Microsoft Store удалён
- [ ] Скачан установщик с python.org
- [ ] При установке ПОСТАВЛЕНА галочка ☑ Add to PATH
- [ ] Команда `python --version` работает в НОВОМ окне PowerShell
- [ ] Команда `python -m pip --version` работает
- [ ] Команда `where python` показывает путь C:\Users\...\Programs\Python\...
- [ ] Зависимости установлены: `python -m pip install -r requirements.txt`
- [ ] Проверка прошла успешно: `python check_setup.py`

---

**Если всё прошло успешно - можете запускать бота! 🎉**

Если возникли проблемы - напишите какая ошибка, помогу разобраться.
